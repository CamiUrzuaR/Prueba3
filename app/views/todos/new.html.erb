<%= form_with model: @todo, local: true do |f| %>
  <%= f.label :title, "To do" %>
  <%= f.text_field :title %>

  <%= f.label :content, "Descripción" %>
  <%= f.text_area :content %>

  <%= f.submit "Enviar" %>
<% end %>
